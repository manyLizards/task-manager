let taskList = [];

class Task {
    constructor(taskName, important, dueDate) {
        this.taskName = taskName;
        this.important = important;
        this.dueDate = ;
        this.importanceLevel = 0
        if (important = true) {
            this.importanceLevel + 1
        }
    }
}